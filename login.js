const status=$('#loginStatus');async function hash(v){const b=new TextEncoder().encode(v);const d=await crypto.subtle.digest('SHA-256',b);return [...new Uint8Array(d)].map(x=>x.toString(16).padStart(2,'0')).join('')}function account(){return JSON.parse(localStorage.getItem('academyAccount')||'null')}function showStatus(t){status.textContent=t}$$('[data-auth]').forEach(b=>b.onclick=()=>{$$('[data-auth]').forEach(x=>x.classList.toggle('active',x===b));$$('[data-auth-panel]').forEach(p=>p.hidden=p.dataset.authPanel!==b.dataset.auth);showStatus('')});$('#registerForm').onsubmit=async e=>{e.preventDefault();const name=$('#registerName').value.trim(),email=$('#registerEmail').value.trim().toLowerCase(),pw=$('#registerPassword').value,cf=$('#registerConfirm').value;if(pw!==cf){showStatus('Las contraseñas no coinciden.');return}localStorage.setItem('academyAccount',JSON.stringify({name,email,passwordHash:await hash(pw)}));localStorage.setItem('academyUser',JSON.stringify({name,email}));showStatus('Cuenta creada. Bienvenido a la Academia.');};$('#loginForm').onsubmit=async e=>{e.preventDefault();const a=account(),email=$('#loginEmail').value.trim().toLowerCase(),pw=$('#loginPassword').value;if(!a||a.email!==email||a.passwordHash!==await hash(pw)){showStatus('Correo o contraseña incorrectos.');return}localStorage.setItem('academyUser',JSON.stringify({name:a.name,email:a.email}));showStatus(`Bienvenido, ${a.name}.`);$('#loginPassword').value='';};$('#recoverForm').onsubmit=async e=>{e.preventDefault();const a=account(),email=$('#recoverEmail').value.trim().toLowerCase(),pw=$('#recoverPassword').value;if(!a||a.email!==email){showStatus('No se encontró una cuenta con ese correo en este navegador.');return}a.passwordHash=await hash(pw);localStorage.setItem('academyAccount',JSON.stringify(a));showStatus('Contraseña actualizada. Ya puede iniciar sesión.');};